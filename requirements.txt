# Phase 1
ultralytics>=8.3
lap>=0.5.12
opencv-python>=4.10
numpy>=1.26
insightface>=0.7.3
# GPU build (CUDA execution provider) on Linux -- e.g. Colab -- so face
# embedding isn't stuck on CPU there; onnxruntime-gpu has no macOS wheel,
# so local/dev machines fall back to the plain CPU build.
onnxruntime-gpu>=1.20; sys_platform == "linux"
onnxruntime>=1.20; sys_platform != "linux"

# Phase 2
torch>=2.4
torchvision>=0.19
transformers>=4.57
accelerate>=1.0
qwen-vl-utils>=0.0.14
